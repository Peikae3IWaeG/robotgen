version: '3'
services:
  frontend:
    build:
      context: ./front
      dockerfile: Dockerfile
    ports:
      - 3000:3000
  
  backend:
    build:
      context: ./back
      dockerfile: Dockerfile
    ports:
      - 5000:5000
    environment:
      - SIMULATE_ENDPOINT=
      - OPENAI_API_KEY=